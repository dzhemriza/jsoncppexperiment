Experiment using jsoncpp library in C++

Dependencies (Ubuntu/Debian):
  sudo apt-get install libjsoncpp-dev libjsoncpp-doc libjsoncpp1

Compilation:
  cd jsoncppexperiment
  mkdir build
  cd build
  cmake ..
  make
